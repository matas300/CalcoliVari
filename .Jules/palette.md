## 2024-05-14 - Add label to password field
**Learning:** Re-using existing tags instead of introducing new custom CSS classes aligns with constraints while improving accessibility for login input. Using a wrapper tag `<p><label>...` works well to preserve original styling on `<p>` while making text function as an input label.
**Action:** Always check if a tag can be wrapped or replaced without changing styling constraints.
