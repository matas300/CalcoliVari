# Audit fiscale — 24 aprile 2026

**Branch:** `dev-taxaudit` · **Scope:** Scadenziario, Dichiarazione PF, Workflow Fatture

Legenda: 🔴 bug fiscale (calcolo/data/regola sbagliata) · 🟡 rischio (edge case, ambiguità) · 🟢 osservazione

---

## 1. Scadenziario

### 🔴 Bug

| File:line | Problema | Regola |
|---|---|---|
| `app.js:4753-4769` | **Saldo anno N non sottrae acconti già versati**: usa `currentApplied.tasse − impostaAcconti.total` (importo *calcolato*, non *pagato*). Anno chiuso OK, anno aperto mostra saldo gonfiato se utente ha già versato acconti. | Cross-year: saldo va calcolato al netto dei pagamenti effettivi registrati. |
| `app.js:4537-4549` | **1° acconto imposta non segue la proroga del saldo**: se `overrideSaldoImposta` sposta il saldo N-1 (es. proroga AdE al 31/8), il 1° acconto N resta hardcoded al 30/6. | Per L. 27/2022 (e DPCM proroghe annuali), saldo + 1° acconto hanno stessa scadenza. L'override deve applicarsi a entrambi (stesso vale per contributi variabili). |

### 🟡 Rischio

| File:line | Problema | Nota |
|---|---|---|
| `app.js:4073` | Soglia unico acconto usa `base < 257.52` (strict). A 257,52 esatti oggi va in split 40/60 invece di unico a novembre. Incoerenza con `tax-engine.js:80` che usa `<=`. | Normativa AdE: se "non inferiore" → `base ≥ 257,52` → split; equivalente a `< 257,52` → unico. App.js è OK, tax-engine.js è troppo permissivo. Verificare quale logica è attiva a runtime. |
| `app.js:4678-4715` | Bollo Q4 anno precedente (scad. 28/2) hardcoded senza `buildRolledDueDate`. Se 28/2 cade sabato/domenica non slitta al lunedì. | DPR 558/1999 art. 1: scadenze fiscali nel weekend slittano al primo giorno lavorativo. Q1-Q3 lo fanno già, Q4-prev no. |
| `app.js:4589-4607` | Saldo contributi N-1 con storico N-1 mancante + nessun `primoAnno*` → saldo=0. Nota UI a riga 4481 invece promette "stima su dati anno N". | Incoerenza comunicativa utente; da allineare o la nota o il calcolo. |

### 🟢 Verificato OK

- Split 40/60 con recupero cents (`tax-engine.js:74-91`)
- Riduzione 35% applicata solo a art-comm (`app.js:4570-4584`)
- Codici F24: 1790/1791/1792/3850/2521-2524 (`app.js:521-528`)
- Date INPS fissi 16/5, 20/8, 16/11, 16/2 (`FORFETTARIO_RULES`)
- Soglia 51,65 € con `<=` corretto
- Bollo trimestrale soglia 77,47 € per operazione + differimento L. 73/2022 (≤ 5.000 €)

---

## 2. Dichiarazione Redditi PF

### 🔴 Bug / missing

| File:line | Problema | Regola |
|---|---|---|
| ~~`dichiarazione-engine.js:251-257`~~ | ~~Signature mismatch~~ **FALSO ALLARME (verificato 2026-04-24)**: il parametro `profile` nell'engine riceve `yearData` dalla UI; siccome `yearData.settings` e `yearData.fatture` esistono, il codice funziona. Ricavi LM2 corretti. Solo naming confuso (il param dovrebbe chiamarsi `yearData`). | Downgrade a 🟢 osservazione: refactor naming opzionale. |
| `dichiarazione-engine.js:99-108` | **Quadro LM incompleto**: calcola LM1-4, LM34, LM36, LM47 ma **manca LM38 (imposta dovuta = LM34 × aliquota)**, **LM40 (1° acconto)**, **LM42 (2° acconto/saldo)**. Senza questi il quadro non produce i valori da riportare in F24. | Istr. Redditi PF 2026 quadro LM sez. II: LM38-42 obbligatori per saldo + acconti. |
| `dichiarazione-engine.js:110-154` | **Quadro RR senza RR40/RR41** (acconti già versati e saldo pregresso). RR8 (saldo) calcolato solo come `RR4 − RR5`, dovrebbe essere `RR4 − RR5 − RR40 − RR41`. | Istr. RR sez. I art-comm. |

### 🟡 Rischio

| File:line | Problema | Regola |
|---|---|---|
| `dichiarazione-engine.js:69-82` | LM3 (contributi deducibili) calcolato **per competenza** (`contribFissi + contribVar` da settings). | Art. 1 c. 64 L. 190/2014: deducibili i contributi **effettivamente versati** (per cassa). Allineare a `buildForfettarioScenario` che è già per cassa, oppure leggere `yearData.pagamenti` tipo=contributi. |
| `dichiarazione-engine.js:54` + test:66-69 | **Aliquota 5% applicata senza verifica requisiti**: basta `settings.impostaSostitutiva=5`, nessun controllo su 5 anni / no attività prec 3 anni / redd dip prec ≤ 30k. | Art. 1 c. 65 L. 190/2014. Serve `validateStartupAliquota()` con warning se requisiti falliscono. |
| `dichiarazione-engine.js:201-214` | **Quadro RW senza check soglia**: accetta qualsiasi conto estero senza distinguere IVAFE (soglia 15.000 € di giacenza media annua) / IVIE (5.000 € valore immobile). Obbligo monitoraggio RW anche sotto soglia se detenuto durante l'anno. | D.L. 167/1990 + Circ. AdE 38/E 2013. |
| `dichiarazione-engine.js:156-178` | **Quadro RS popolato senza disclaimer**: RS371-RS381 sono **dati informativi** per forfettari, non deducono nulla. UI potrebbe far credere siano deducibili. | Istr. RS forfettario. Aggiungere nota esplicita in UI. |
| `dichiarazione-engine.js:89-90` | `perditePregresse` sottratte da LM34 senza check scadenza temporale. | Art. 84 TUIR: perdite riportabili 5 anni (regime ordinario pre-2006 diverse). Warning se perdite > 4 anni. |
| `dichiarazione-engine.js:313-317` | Limite forfettario hardcoded 85k/100k invece di `settings.limiteForfettario`. | Art. 1 c. 54 L. 190/2014 (85k standard, 100k per scenari specifici). Valore da settings. |

### 🟢 OK

- `validateCodiceFiscale` algoritmo ODD/EVEN + check digit conforme (`dichiarazione-engine.js:321-338`)

---

## 3. Workflow Fatture

### 🔴 Bug fiscali

| File:line | Problema | Regola |
|---|---|---|
| `fatture-docs-feature.js:1700` | **Anagrafica cessionario**: usa sempre `<Denominazione>` anche per persone fisiche IT. XSD FatturaPA richiede **`<Nome>` + `<Cognome>`** per PF, `<Denominazione>` solo per PG. XML attuale sarebbe rifiutato da SdI per cliente PF IT. | `1.4.1.3.1` del tracciato FatturaPA v1.2. |
| `fatture-docs-feature.js:1529` | **Natura N2.1 per clienti esteri** è sbagliata per forfettario. | AdE Guida FE 2020: forfettario → `N2.2` sempre (non soggette per art. 1 c. 58 L. 190/2014, non per art. 7-7septies). N2.1 riservata a operazioni extraterritoriali IVA ordinario. |
| `fatture-docs-feature.js:1717` | **XSD element order**: se `DatiRitenuta` manca, `DatiBollo` potrebbe finire prima di `ImportoTotaleDocumento` correttamente, ma il template string non garantisce ordine canonico Numero → DatiRitenuta → DatiBollo → ImportoTotaleDocumento → Causale in tutti i rami. | XSD `fatturaordinaria_v1.2.xsd`. Refactor con emissione condizionale ordinata. |

### 🟡 Rischio

| File:line | Problema | Nota |
|---|---|---|
| `fatture-docs-feature.js:1489` | `sanitizeProgressivoInvio` tronca silenziosamente a 10 char. Numero `"2026/0001"` → `"202600001"` (9 char, OK); `"FATT/2026/001"` → `"FATT202600"` (perde il progressivo). | Aggiungere warning utente o validazione upfront: numero sanitized troncato ≠ originale → errore. |
| `fatture-docs-feature.js:1521-1524` | Nome/Cognome cedente estratto con `split(/\s+/).slice(-1)` — fragile: "Mario Rossi Bianchi" → Nome="Mario Rossi", Cognome="Bianchi". | `settings.anagrafica.nome` e `settings.anagrafica.cognome` dovrebbero già essere separati dopo migrazione C4. Verificare che sia così. |
| `fatture-docs-feature.js:1018-1024` | Validazione data NC ≥ data originale solo in `saveFatturaDraft`, non in `previewFatturaXml`/`downloadFatturaXml`. Utente può scaricare XML NC antidatato senza trigger. | Spostare check in `buildFatturaElettronicaXml`. |
| `fatture-docs-feature.js:378` | UI `syncBolloDefault` accende bollo su `subtotal + contributoIntegrativo > 77,47`; XML builder (linea 1566) usa solo `subtotal`. Mismatch se contributo > 0. | Forfettario ha contributo bloccato a 0 (riga 1446), quindi mismatch solo teorico oggi. Se si sbloccasse (casse autonome) emergerebbe. |
| `fatture-nc-sync.js:15` | Tolleranza 0,01 € per totale ok ma test di idempotenza: richiamare `applyNCToOriginal` sulla stessa NC 2 volte → check ncIds protegge, ma se nc.id muta (es. save bozza→inviata con nuovo id) si raddoppia. | Verificare stabilità id NC nel ciclo di vita. |

### 🟢 OK

- `validateCodiceFiscale` / `isValidPartitaIvaIT` corretti
- Bollo soglia 77,47 € per documento applicata correttamente (riga 1566)
- `ImportoRitenuta` con segno (TD04 negativo), `AliquotaRitenuta` positiva
- `DatiFattureCollegate` obbligatorio su TD04 presente
- `RegimeFiscale=RF19` forfettario corretto
- `ImportoPagamento = totale − ritenuta` corretto

---

## Sintesi fix prioritari

1. **Verificare signature `buildDichiarazione`** (ui ↔ engine) — se confermato, i ricavi LM2 sono potenzialmente sbagliati
2. **Anagrafica XML cessionario PF**: emettere `<Nome>/<Cognome>` invece di `<Denominazione>`
3. **Natura N2.2 sempre** (rimuovere N2.1 per esteri)
4. **Completare quadro LM** (LM38, LM40, LM42)
5. **Completare quadro RR** (RR40, RR41)
6. **Saldo anno N** → sottrarre acconti effettivamente pagati
7. **1° acconto imposta** → applicare override saldo
8. **Contributi deducibili LM3** → per cassa (non competenza)
9. **Validazione aliquota 5% start-up** con warning su requisiti
10. **Quadro RW soglie IVAFE/IVIE** + warning detenzione

Fix 2-3 sono **blocker** per emissione XML conforme (TD01 a PF e NC a esteri).
Fix 4-5 sono **blocker** per produrre dichiarazione utilizzabile.
